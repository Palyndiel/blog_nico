<div class="well">
  {{ form_start(formAns, {'attr': {'class': 'form-horizontal', 'full_name': 'answer'}}) }}

  {# Les erreurs générales du formulaire. #}
  {{ form_errors(formAns) }}

<ul>
        <div class="form-group">
            {# Génération du label. #}
            {{ form_label(formAns.content, "Votre réponse", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}

            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(formAns.content) }}

            <div class="col-sm-10">
              {# Génération de l'input. #}
              {{ form_widget(formAns.content, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>
    
        {# Pour le bouton, pas de label ni d'erreur, on affiche juste le widget #}
        {{ form_widget(formAns.save, {'attr': {'class': 'btn btn-primary'}}) }}
</ul>

  {# Génération automatique des champs pas encore écrits.
     Dans cet exemple, ce serait le champ CSRF (géré automatiquement par Symfony !)
     et tous les champs cachés (type « hidden »). #}
  {{ form_rest(formAns) }}

  {# Fermeture de la balise <form> du formulaire HTML #}
  {{ form_end(formAns) }}
</div>